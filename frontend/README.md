# Task Manager — Frontend

React 19 + Vite 6, plain CSS. Connects to the backend API on port 3001.

## Dev

```bash
npm install
npm run dev   # http://localhost:5173
```

## Build

```bash
npm run build   # output → dist/
```
